import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";

import Button, { ButtonAppearance } from "./Button";

<Meta
  title="Button"
  component={Button}
  argTypes={{
    children: {
      control: {
        type: "text",
      },
    },
    appearance: {
      control: {
        options: ButtonAppearance,
        type: "select",
      },
    },
  }}
  args={{
    children: "Click me!",
    appearance: ButtonAppearance.DEFAULT,
  }}
/>

export const Template = (args) => <Button {...args} />;

### Button

This is a [React](https://reactjs.org/) component for the Vanilla [Button](https://docs.vanillaframework.io/patterns/buttons/).

Buttons are clickable elements used to perform an action, they can be used for buttons and link elements.

### Props

<ArgsTable of={Button} />

### Default

<Canvas>
  <Story
    name="Default"
    args={{
      children: "Button",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Default disabled"
    args={{
      children: "Button",
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Base" args={{ children: "Base button", appearance: "base" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Base disabled"
    args={{
      children: "Base button disabled",
      appearance: "base",
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Link"
    args={{
      children: "Link button",
      appearance: "base",
      element: "a",
      href: "#test",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Link disabled"
    args={{
      children: "Link button disabled",
      appearance: "base",
      element: "a",
      href: "#test",
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Positive"
    args={{ children: "Positive button", appearance: "positive" }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Positive disabled"
    args={{
      children: "Positive button disabled",
      appearance: "positive",
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Negative"
    args={{ children: "Negative button", appearance: "negative" }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Negative disabled"
    args={{
      children: "Negative button disabled",
      appearance: "negative",
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Brand" args={{ children: "Brand button", appearance: "brand" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Brand disabled"
    args={{
      children: "Brand button disabled",
      appearance: "brand",
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Inline">
    {() => (
      <>
        <span>Everything you need to get started with Vanilla. </span>
        <Button appearance="neutral" inline>
          Inline button
        </Button>
      </>
    )}
  </Story>
</Canvas>

<Canvas>
  <Story name="Dense">
    <>
      <span>Everything you need to get started with Vanilla. </span>
      <Button dense>Dense button</Button>
    </>
  </Story>
</Canvas>

<Canvas>
  <Story name="Small">
    {() => (
      <>
        <Button small>Small button</Button>
        <Button small dense>
          Small dense button
        </Button>
      </>
    )}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Icon"
    args={{ children: <i className="p-icon--plus"></i>, hasIcon: true }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Icon & text"
    args={{
      children: (
        <>
          <i className="p-icon--plus"></i> <span>Button with icon & text</span>
        </>
      ),
      hasIcon: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
