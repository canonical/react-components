import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs/blocks";

import Chip from "./Chip";

<Meta title="Chip" component={Chip} />

export const Template = (args) => <Chip {...args} />;

### Chip

This is a not an existing Vanilla component. It can be used to display a small value attached to a component.

### Props

<ArgsTable of={Chip} />

### Default

<Canvas>
  <Story
    name="Default"
    args={{
      value: "positive",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Key-value

<Canvas>
  <Story name="Lead-value">
    <Chip lead="Owner" value="Bob" />
  </Story>
</Canvas>

### Dismissible

<Canvas>
  <Story name="Dismissible">
    <Chip
      lead="Owner"
      value="Bob"
      onDismiss={() => {
        console.log("onDismiss called");
      }}
    />
  </Story>
</Canvas>
